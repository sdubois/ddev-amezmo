#ddev-generated
# Amezmo staging provider. Pushes can overwrite remote data. Every connection value is independent;
# shared-container plans may repeat production's SSH values, while isolated
# plans may use a different host and port.

info_command:
  service: host
  command: |
    "${DDEV_APPROOT}/.ddev/amezmo/amezmo" --profile staging info

auth_command:
  service: host
  command: |
    "${DDEV_APPROOT}/.ddev/amezmo/amezmo" --profile staging auth

db_pull_command:
  service: host
  command: |
    "${DDEV_APPROOT}/.ddev/amezmo/amezmo" --profile staging pull-db

files_import_command:
  service: host
  command: |
    "${DDEV_APPROOT}/.ddev/amezmo/amezmo" --profile staging pull-files

db_push_command:
  service: host
  command: |
    "${DDEV_APPROOT}/.ddev/amezmo/amezmo" --profile staging push-db

files_push_command:
  service: host
  command: |
    "${DDEV_APPROOT}/.ddev/amezmo/amezmo" --profile staging push-files
